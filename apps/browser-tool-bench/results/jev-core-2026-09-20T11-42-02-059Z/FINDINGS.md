# 当前结果与待完成工作

本批次在第 8 次运行被连接错误打断，计划 36 次尚未完成。这里的速度对比均只有同题同轮的一对，不能代替三轮中位数。

| 任务 | Jev 优先 | Kimi SDK + Jev 辅助 | 原题结果 | Jev 执行路径 |
| --- | ---: | ---: | --- | --- |
| T05 动态列表 | 62.518 秒 | 130.382 秒 | 两组通过 | 先 Jev 等待，后完整 SDK |
| T06 分页商品 JSON | 105.251 秒 | 135.020 秒 | 两组答案与页面操作正确 | Jev 点击第二页，后完整 SDK |
| T15 SSE 实时流 | 10.056 秒 | 66.279 秒 | 两组通过 | Jev 操作 + 一次 Kimi 文本核验，无完整 SDK |
| T20 十次检查 | 连接失败 | 88.014 秒 | 只有原流程通过 | 首次观察失败，还没调用 Jev |

## 已有证据支持什么

- T15 是目前最清晰的短路径收益：Jev 共 4 次调用、约 2.084 秒；Kimi 文本核验 1 次、约 3.743 秒。完整耗时还含导航、观察、等待、收尾，不能把这些分项全部当作互不重叠后相加。
- T05 两组浏览器工具累计时间分别约 12.285 秒和 16.113 秒，小于总耗时差异；不能将剩余约一分钟差异直接解释成浏览器或 Jev 模型更快。SDK 启动、模型推理和输出都参与了总时间。
- T06 原流程导出了正确数据，但完成校验两次失败，最终产品状态是 partial。第一回证据被改写为非页面原文；第二回包含少于三个字符的证据。报告同时保留原题正确与产品完成两种口径。
- T06 Jev 组也出现过 finish / reconcile 校验错误，后续自行完成，时间已经包含这些纠正。没有人工提供答案或补做操作。
- T20 失败来自 Wrapper 在首次观察时返回 `CDP Session 不属于当前 Agent 连接`，未发起 Jev 请求。这是连接层阻塞，不能作为 Jev 模型判断失败，更不能把 3.766 秒当成提速。

## 后续排查方向（尚未实施）

1. 先统一 Gateway 运行版本并验证会话连续切换。当前服务 PID 26328，自 2026-09-15 启动、协议 14；本地代码协议 15。仅改变测试准备会话没有消除错误，版本落后是否为根因仍未证实。
2. 当前 fast observation 用控件 guard 作为 fingerprint。控件不变、正文继续加载时，可能被判为连续无变化并过早回退；动作有效性与任务进度应分别判断。
3. 结构化提取经常进入完整 SDK，Jev 判断动作本身快并不能自动缩短数据整理与输出。应评估独立提取路径，而不是默认声称全部任务都能由 Jev 加速。
4. 完成证据校验需要更清楚的工具反馈或结构化引用，防止正确结果因标点、空白或短证据变成 partial；不能简单取消完成核验。

## 等待中的操作

已向用户请求：允许重启 ProfilePilot 的浏览器连接服务并重新跑完整 36 次。重启会影响 9223、9224、9226、9227 上保留的窗口和会话，需要先保存页面内容；未获得回复前不执行。

当前 9227 仍保留本批次暂停的 `pp-task-c63f08b9-6d82-42da-b5bf-df641a4d881c` 会话。最后的 user_takeover 事件由测试脚本在连接错误后调用 pause 产生，不代表已证实用户点击接管。未自动 resume 或另换端口。

新批次脚本已加入 Gateway 版本匹配检查及批次内 PID/版本一致性检查。正式重测应使用新的结果目录，旧预试验和本次中断记录完整保留，不与升级后的数字混算。

报告由独立进程提供：http://127.0.0.1:4401/ 。ProfilePilot 桌面应用仍在运行。
