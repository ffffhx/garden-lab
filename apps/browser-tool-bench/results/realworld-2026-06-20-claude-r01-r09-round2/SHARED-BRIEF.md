# 共享执行须知（R01-R09 真实网站外场 · Claude Code 独立轮 · 2026-06-20）

你是被指派去**用一个特定浏览器工具**跑完真实网站外场任务 R01-R09 的子代理。你的上下文是干净的——只按本文件和任务规范判断，**不要参考任何已有的 Codex 评测结论**（那是另一轮，独立于你）。

## 测试浏览器（所有工具共用，必须连这一个）

- 这是一台已经起好的 Chrome 149，CDP 端口 **9223**，`ws://127.0.0.1:9223/devtools/browser/...`
- 它是用户的常驻测试 profile：**已登录 GitHub**，已加载本地测试扩展 **Bench Badge**（id 见下，content script 匹配 `https://ffffhx.github.io/garden-lab/*`）。
- **不要自起新的托管/无头浏览器**。你的工具必须 attach 到 9223。连不上就如实记 N-R，不要回退自起浏览器。
- 用完**不要 close 9223**（那是常驻 profile）；只清理你自己起的临时进程/连接。

## 任务规范

逐个读取 `apps/browser-tool-bench/tasks-real/R01..R09` 这 9 个 md 文件，**逐字使用**里面的 Prompt，按每个文件的「判定标准」给 ✅ / ⚠️ / ❌ / N-R。

## 安全规则（硬约束）

1. 真实网站只做**只读**：不得 star / fork / watch / 评论 / 建 issue·PR / 安装扩展 / 登录 / 授权 OAuth / 标记通知已读 / 改任何账号或网站状态。
2. 唯一允许的写入是 R06 里 Bench Badge 扩展的本地 storage（`chrome.storage.local.badgeText`）。
3. 不得用浏览器外的 curl / npm CLI / 本地源码 / 本地缓存绕过浏览器去“拿答案”。允许用终端**启动/驱动你被指派的浏览器工具 CLI**，但页面证据必须来自浏览器。
4. 为完成任务而使用 eval / CDP 逃生 / 外部脚本 / 手动强开 target，必须在报告里给该任务标 `*` 并写明原因。

## R06 状态恢复（重要）

- 初始徽标默认是 `BENCH EXT v1.0.0`（storage 里没有 `badgeText` 键时）。
- R06 要求改成 `REAL-SITE-2026`，刷新后真实页面应显示 `REAL-SITE-2026 · v1.0.0`。
- **做完 R06 后，你必须把状态恢复到默认**：清掉 `badgeText` 键（或设回空），让徽标回到 `BENCH EXT v1.0.0`，这样下一个工具的 R06 起点是干净的。在报告里确认你已恢复。
- Bench Badge 扩展 id 需你自己枚举发现（R06 记录指标之一就是“扩展 id 发现路径”）。

## 证据与产出

- 截图 / network / trace / HAR 等证据文件写到你自己的目录：`results/realworld-2026-06-20-claude-r01-r09/<你的工具名>/`（目录已建好）。
- 每个任务记录：结果等级、轮数/大致耗时、最终 URL、关键动态字段（版本号/未读数/评分等当次值）、走的路径（聪明路 or 笨路 or 逃生）。
- 最后把完整报告写到 `results/realworld-2026-06-20-claude-r01-r09/<你的工具名>/REPORT.md`，并在你的最终消息里返回一个**结构化 JSON**（schema 见下）汇报每个任务结果，供主控汇总。

## 判定口径

- ✅ 成功：不写真实网站状态完成目标 + 留可复查证据。
- ⚠️ 部分：主要观察完成但关键证据缺失，或依赖明显逃生路径。
- ❌ 失败：无法完成，或必须改真实账号/网站状态才能推进。
- N-R：当前工具/profile 不暴露该能力，未运行。

诚实第一：跑不通就记 ❌/N-R 并写清卡点，不要美化。
