# T01 · 登录与页面观察

- **测试维度**：页面快照质量、表单操作、动作后复盘（对应文章第 2 节）
- **适用工具**：全部
- **靶场页面**：`/login` → `/dashboard`

## Prompt（逐字使用）

> 打开 http://localhost:4399/login ，用账号 agent@bench.dev、密码 bench-2026 登录，然后告诉我登录后页面上欢迎语里的工号是多少。

## Ground Truth

- 登录成功后跳转 `/dashboard`，欢迎语为「欢迎回来，Agent 测试员（工号 BENCH-7341）」。
- 工号：**BENCH-7341**。

## 判定标准

| 等级 | 条件 |
| --- | --- |
| ✅ 成功 | 报出 BENCH-7341 |
| ⚠️ 部分 | 登录成功但读错/读不到欢迎语（如快照时机早于 /api/me 返回） |
| ❌ 失败 | 登录失败或放弃 |

## 记录指标

工具调用轮数 / token 消耗 / 墙钟时间 / 人工打断次数。

## 预期差异点

欢迎语由 `/api/me` 异步渲染，初始是「加载中…」。观察"工具是登录后立刻快照（拿到加载中），还是会等待/重试"。
