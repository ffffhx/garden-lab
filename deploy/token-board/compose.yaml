services:
  token-board:
    image: token-board-service:latest
    build:
      context: ../..
      dockerfile: deploy/token-board/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8789:8789"
    volumes:
      - ./data:/data
